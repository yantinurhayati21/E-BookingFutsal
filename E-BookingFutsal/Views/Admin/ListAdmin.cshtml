@model List<Admin>
@{
    ViewData["Title"] = "List Admin";
    Layout = "_LayoutAdmin";
}
<partial name="_Notification" />
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>List Admin</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/css/bootstrap.min.css" />
    <!-- DataTables CSS -->
    <link rel="stylesheet" href="~/css/datatables.min.css" />
</head>

<body>
    <div class="header--wrapper">
        <div class="header--title">
        </div>
        <div class="user--info">
            <img src="~/image/img.jpeg" alt="">
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3>List Admin</h3>
                    </div>
                    <div class="card-body">
                        <div class="col-6 text-start">
                            <a asp-controller="Admin" asp-action="Create" class="btn btn-primary">
                                <i class="fas fa-plus"></i> Add Admin
                            </a>
                        </div>
                        <div class="data_table">
                            <table id="example" class="table table-striped table-bordered">
                                <thead class="table-dark">
                                    <tr>
                                        <th>Nama Admin</th>
                                        <th>Username</th>
                                        <th>Email</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var admins in Model)
                                    {
                                        <tr>
                                            <td>@admins.Nama</td>
                                            <td>@admins.Username</td>
                                            <td>@admins.Email</td>
                                            <td>
                                                <a href="/Admin/Detail/@admins.Id" class="btn btn-primary"><i class="fa fa-bookmark" aria-hidden="true"></i> Detail</a>
                                                <a href="/Admin/Update/@admins.Id" class="btn btn-warning mx-5"><i class="fa fa-pencil" aria-hidden="true"></i> Update</a>
                                                <a href="/Admin/Delete/@admins.Id" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i> Delete</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- JavaScript Files -->
    <script src="~/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/jquery-3.6.0.min.js"></script>
    <script src="~/js/datatables.min.js"></script>
    <script src="~/js/pdfmake.min.js"></script>
    <script src="~/js/vfs_fonts.js"></script>
    <script src="~/js/custom.js"></script>
</body>

</html>
